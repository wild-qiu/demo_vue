<template>
<div :class="{comfirmchange: !isshow,comfirm: isshow}" >
      <div class="confirm-content-wrap" >
        <h3 class="my-confirm-title">{{ titleText }}</h3>
         <p class="my-confirm-content">{{ content }}</p>
        <div class="my-operation">
          <div class="confirm-btn"  @click="isshow=!isshow">
            <p class="my-btn-text">{{ confirmText }}</p>
          </div>
        </div>
      </div>
    
  </div>
</template>

<script type="text/ecmascript-6">
export default {
    name:'comfirm',
  data () {
    return {
            isshow: true,
            titleText: '提示', // 提示框标题
            content: '该功能尚未开发！', // 提示框的内容
            confirmText: '确认', // 确认按钮的文字
    }
  },
  methods: {
               changeStyle:function(index){
                this.changeSelectStyle = index;
                },
  }
}
</script>

<style scoped>
.comfirmchange{
    display: none;
}
.comfirm{
    width: 100%;
}
  .my-confirm {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: 998;
    /* 这里防止当用户长按屏幕，出现的黑色背景色块，以及 iPhone 横平时字体的缩放问题 */
  }

  /* 包裹层容器样式 */
  .confirm-content-wrap {
    position: absolute;
    top: 28%;
    left: 0;
    right: 0;
    width: 250px;
    margin: 0 auto;
    background-color: #fff;
    border-radius: 5px;
    z-index: 999;
    user-select: none;
     border: 1px solid silver;
  }

  /* 顶部标题部分 */
  .my-confirm-title {
    padding-top: 4px;
    text-align: center;
    font-size: 20px;
    font-weight: 500;
    color: #333;
  }

  /* 中间内容部分 */
  .my-confirm-content {
    padding: 0 15px;
    padding-top: 4px;
    margin-bottom: 20px;
    text-align: center;
    font-size: 14px;
    color: #666;
    line-height: 1.5;
  }

  /* 底部按钮样式 */
  .my-operation {
    display: flex;
    border-top: 1px solid #eee;
  }
  .my-operation .my-cancel-btn, .confirm-btn {
    flex: 1;
  }
  .my-operation .confirm-btn {
    color: #ffb000;
  }
  .my-operation .my-btn-text {
    text-align: center;
    font-size: 16px;
    margin: 10px 0;
    padding: 6px 0;
  }

  /* 其他修饰样式 */
  .my-border-right {
    border-right: 1px solid #eee;
  }

  
</style>
